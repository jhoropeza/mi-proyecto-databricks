# .github/workflows/ci-cd-databricks.yml
# ----------------------------------------------------------------------
name: Databricks CI/CD con Aprobación

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  # Variables globales de la ruta del código y del workspace de Databricks
  NOTEBOOK_PATH: notebooks/ingestion/main_ingestion.py
  DBR_WORKSPACE_PATH: /Users/ci-cd/${{ github.repository_id }}/main_ingestion
  PYTHON_VERSION: '3.9' 

jobs:
  # ===================================================================
  # 1. INTEGRACIÓN CONTINUA (CI) & DESPLIEGUE A DEV
  # ===================================================================
  build_and_deploy_dev:
    name: 'CI, Test & Despliegue a DEV'
    runs-on: ubuntu-latest
    environment: dev 
    
    steps:
      - name: 'Checkout del Código'
        uses: actions/checkout@v4
      
      # 1. CONFIGURACIÓN DEL ENTORNO DE PRUEBA
      - name: '1. Configurar Python ${{ env.PYTHON_VERSION }}'
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          
      - name: '2. Instalar Dependencias (requirements.txt)'
        run: pip install -r requirements.txt
        
      # 2. EJECUCIÓN DE PRUEBAS
      - name: '3. Ejecutar Pruebas (pytest)'
        run: pytest tests/
        
      # 3. DESPLIEGUE A DATABRICKS DEV
      - name: '4. Instalar Databricks CLI'
        uses: databricks/setup-cli@main # <--- CORREGIDO A @main
        
      - name: '5. Desplegar Notebook a DEV'
        uses: databricks/cli-action@main # <--- CORREGIDO A @main
        with:
          databricks-host: ${{ secrets.DBR_HOST_DEV }}
          databricks-token: ${{ secrets.DBR_TOKEN_DEV }}
          cli-command: |
            # Despliegue a la carpeta de DEV
            databricks workspace import ${{ env.NOTEBOOK_PATH }} /Users/ci-cd/${{ github.repository_id }}/DEV/${{ github.sha }} --overwrite
            echo "Notebook desplegado en DEV: /Users/ci-cd/${{ github.repository_id }}/DEV/${{ github.sha }}"
            
  # ===================================================================
  # 2. DESPLIEGUE CONTINUO (CD) A PRODUCCIÓN
  # ===================================================================
  deploy_to_prod:
    name: 'CD a PRODUCCIÓN (Requiere Aprobación)'
    runs-on: ubuntu-latest
    needs: [build_and_deploy_dev] 
    
    environment: 
      name: prod # 👈 ACTIVA la regla de aprobación en GitHub
      
    steps:
      - name: 'Checkout del Código'
        uses: actions/checkout@v4
      
      - name: '1. Instalar Databricks CLI'
        uses: databricks/setup-cli@main # <--- CORREGIDO A @main
        
      - name: '2. Desplegar Notebook a PROD'
        uses: databricks/cli-action@main # <--- CORREGIDO A @main
        with:
          databricks-host: ${{ secrets.DBR_HOST_PROD }}
          databricks-token: ${{ secrets.DBR_TOKEN_PROD }}
          cli-command: |
            # Despliegue a la carpeta final de PROD
            databricks workspace import ${{ env.NOTEBOOK_PATH }} /Users/ci-cd/${{ github.repository_id }}/PROD/final --overwrite
            echo "Notebook desplegado en PROD: /Users/ci-cd/${{ github.repository_id }}/PROD/final"